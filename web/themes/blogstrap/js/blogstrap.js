/**
 * @file
 * JavaScript for Blogstrap theme.
 */

(function (Drupal) {
  'use strict';

  /**
   * Attach behaviors for Blogstrap theme.
   */
  Drupal.behaviors.blogstrap = {
    attach: function (context, settings) {
      // Add custom JavaScript behaviors here.
    }
  };

})(Drupal);
